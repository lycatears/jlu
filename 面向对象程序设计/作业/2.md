## 整理函数工作机制，理解应用程序运行时内存管理机制

- 我的答案：80分

程序运行时，全局变量存放在data区，函数的局部变量存放在栈区，动态变量存放在堆区，函数代码存放在代码区。当调用一个函数时，将开辟一个栈区空间，从主调函数中将实参传递给形参，给形参分配内存单元，函数执行完毕后释放这个栈区空间。

- 正确答案：

内存管理的四个区（代码区、全局数据区、栈区、堆区）；栈顶的工作活动情况